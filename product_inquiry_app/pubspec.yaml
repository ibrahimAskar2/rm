name: product_inquiry_app
description: تطبيق استعلام المنتجات من Supabase

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^1.10.18

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
